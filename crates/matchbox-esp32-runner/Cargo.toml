[package]
name = "matchbox-esp32-runner"
version = "0.1.0"
edition = "2024"

[features]
default = [
    "strict-profile",
    "platform-web",
    "platform-mdns",
    "platform-camera",
    "platform-bluetooth",
    "platform-pins",
    "platform-sdcard",
    "platform-printer",
]
embedded-web = ["platform-web"]
strict-profile = []
platform-web = []
platform-mdns = ["platform-web"]
platform-camera = []
platform-bluetooth = ["dep:esp32-nimble"]
platform-pins = []
platform-sdcard = []
platform-printer = ["platform-bluetooth"]
psram = []

[dependencies]
matchbox-embedded = { path = "../matchbox-embedded" }
matchbox_compiler = { path = "../matchbox-compiler" }
matchbox_vm = { path = "../matchbox-vm" }
anyhow = "1.0"
postcard = { version = "1.0", features = ["alloc", "use-std"] }
esp-idf-svc = { version = "=0.51.0", features = ["binstart"] }
esp-idf-sys = "=0.36.1"
esp32-nimble = { version = "=0.10.1", optional = true }
embedded-svc = "=0.28.1"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
base64 = "0.22"
url = "2.5"
uuid = "1.8"
jpeg-decoder = "0.3"

[build-dependencies]
embuild = { version = "0.33", features = ["espidf"] }

[package.metadata.esp-idf-sys]

[[package.metadata.esp-idf-sys.extra_components]]
remote_component = { name = "espressif/mdns", version = "1.5" }

[[package.metadata.esp-idf-sys.extra_components]]
remote_component = { name = "espressif/esp_tinyusb", version = "1.1" }

[[package.metadata.esp-idf-sys.extra_components]]
bindings_header = "bindgen-shims/portmacro_bindgen.h"

[profile.release]
opt-level = "s" # Optimize for size
lto = true
codegen-units = 1
panic = "abort"
debug = "line-tables-only"
strip = false
