// boxlang_compat: ortus/boxlang/runtime/bifs/global/query/QueryEachTest.java
q = queryNew( "a" );
queryAddRow( q, { a: 1 } );
queryAddRow( q, { a: 2 } );
acc = 0;
queryEach( q, function( row ) { acc = acc + row.a; } );
assert acc == 3 : "testEach";
