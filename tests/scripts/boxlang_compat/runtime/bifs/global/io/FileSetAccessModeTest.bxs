// boxlang_compat: ortus/boxlang/runtime/bifs/global/io/FileSetAccessModeTest.java
// SKIPPED [boxlang_compat] (JVM-only, hard requirement): open BoxFile and
// Java POSIX-permission assertions remain omitted; path calls and errors execute below.
p = createTempFile();
fileWrite( p, "file access mode test" );
fileSetAccessMode( p, 777 );
assert fileExists( p ) : "testFilePathSetAccessMode exists";
threw = false;
try {
    fileSetAccessMode( p, "blah" );
} catch ( e ) {
    threw = true;
}
assert threw : "testModeError";
fileDelete( p );
