// boxlang_compat: ortus/boxlang/runtime/bifs/global/io/FileWriteLineTest.java
// SKIPPED [boxlang_compat] (JVM-only, hard requirement): open BoxFile/member
// assertions remain omitted; the string-path overload executes below.
p = createTempFile();
fileWriteLine( p, "box" );
fileWriteLine( p, "lang" );
result = fileRead( p );
assert fileExists( p ) : "testWriteLineString exists";
assert len( result ) > 0 : "testWriteLineString content";
fileDelete( p );
