// boxlang_compat: ortus/boxlang/runtime/bifs/global/io/FileIsEOFTest.java
// SKIPPED [boxlang_compat] (JVM-only, hard requirement): open BoxFile stream
// state assertions remain omitted; the path error executes below.
p = createTempFile();
fileWrite( p, "eof" );
threw = false;
try {
    fileIsEOF( p );
} catch ( e ) {
    threw = true;
}
assert threw : "testReadFileEOF path-requires-open-file";
fileDelete( p );
