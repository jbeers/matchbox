// boxlang_compat: ortus/boxlang/runtime/bifs/global/io/FileSeekTest.java
// SKIPPED [boxlang_compat] (JVM-only, hard requirement): open BoxFile stream
// and Java byte[] assertions remain omitted; the path error and alias execute below.
p = createTempFile();
fileWrite( p, "abcdefg" );
threw = false;
try {
    fileSeek( p, 2 );
} catch ( e ) {
    threw = true;
}
assert threw : "testSeekFile path-requires-open-file";
threwAlias = false;
try {
    fileSkipBytes( p, 2 );
} catch ( e ) {
    threwAlias = true;
}
assert threwAlias : "testSkipBytesFile path-requires-open-file";
fileDelete( p );
