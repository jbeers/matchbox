// ---------------------------------------------------------------------------
// boxlang_compat: ortus/boxlang/runtime/dynamic/ReferencerTest.java
// ---------------------------------------------------------------------------
// SKIPPED [boxlang_compat] (JVM-only / internal BoxLang behavior, hard
// requirement): the JVM test exercises the internal Java ReferencerTest API directly
// (monadic Attempt, ExpressionInterpreter, functional-interface interop, the
// Referencer resolver). No BoxLang source is evaluated, so there is nothing
// to transfer as a black-box script.
// ---------------------------------------------------------------------------
