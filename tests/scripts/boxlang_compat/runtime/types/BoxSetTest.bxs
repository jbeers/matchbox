// ---------------------------------------------------------------------------
// boxlang_compat: ortus/boxlang/runtime/types/BoxSetTest.java
// ---------------------------------------------------------------------------
// SKIPPED [boxlang_compat] (JVM-only / internal BoxLang behavior, hard
// requirement): the JVM test drives the internal Set type API (add/remove/union/intersect) and asserts Java iteration; MatchBox has no Set type yet. No BoxLang source is evaluated, so there is nothing
// to transfer as a black-box script. Revisit when the underlying type lands
// in MatchBox and its behavior is observable from BoxLang source.
// ---------------------------------------------------------------------------
