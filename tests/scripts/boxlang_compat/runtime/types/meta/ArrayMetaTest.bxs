// ---------------------------------------------------------------------------
// boxlang_compat: ortus/boxlang/runtime/types/meta/ArrayMetaTest.java
// ---------------------------------------------------------------------------
// SKIPPED [boxlang_compat] (JVM-only / internal BoxLang behavior, hard
// requirement): the JVM test asserts internal Array metadata via the Java metadata API. No BoxLang source is evaluated, so there is nothing
// to transfer as a black-box script. Revisit when the underlying type lands
// in MatchBox and its behavior is observable from BoxLang source.
// ---------------------------------------------------------------------------
