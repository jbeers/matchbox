// ---------------------------------------------------------------------------
// boxlang_compat: ortus/boxlang/runtime/types/StructTest.java
// ---------------------------------------------------------------------------
// SKIPPED [boxlang_compat] (JVM-only / internal BoxLang behavior, hard
// requirement): the JVM test drives internal Struct mechanics (linked/sorted/case-sensitive variants, keySet order, hashCode, toString). No BoxLang source is evaluated, so there is nothing
// to transfer as a black-box script. Revisit when the underlying type lands
// in MatchBox and its behavior is observable from BoxLang source.
// ---------------------------------------------------------------------------
