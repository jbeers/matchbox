import tests.models.ISpeakable;

interface IGreetable {
    function greet(name);
}

class Person implements="IGreetable, ISpeakable" {
    function greet(name) {
        println("Hello, " & name);
    }
}

p = new Person();
p.greet("Jacob");
p.speak();
