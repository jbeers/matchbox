class BenchPoint {
    property x;
    property y;

    function init(x, y) {
        this.x = x
        this.y = y
        return this
    }

    function sum() {
        return this.x + this.y
    }
}
